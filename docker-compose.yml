version: '3.8'
services:
  whatsapp-helper:
    image: whatsapp-helper:latest
    hostname: whatsapp-helper-container
    volumes:
      - ./docker_dir:/app/auth
      - ./logs:/app/logs
    stdin_open: true
    tty: true
    restart: always